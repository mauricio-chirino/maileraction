<!DOCTYPE html>
<html>
  <head>
    <title>MailerAction</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Semantic UI CSS desde CDN -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/semantic-ui@2.5.0/dist/semantic.min.css">

    <!-- Estilos personalizados (Sprockets) -->
    <%= stylesheet_link_tag "application", media: "all", "data-turbo-track": "reload" %>

    <!-- Lordicon (para íconos animados) -->
    <script src="https://cdn.lordicon.com/lordicon.js"></script>

   <!-- Google Fonts al estilo Semantic UI -->
<link href="https://fonts.googleapis.com/css2?family=Lato:wght@400;700&family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">

  </head>

  <body>
    <!-- Fondo animado -->
    <div id="tsparticles" style="position: fixed; top: 0; left: 0; z-index: -1; width: 100%; height: 100%;"></div>

    <!-- Encabezado -->
    <header class="">
      
        <%# <a href="/" class="header item">MailerAction</a>
        <a href="#planes" class="item">Planes</a>
        <a href="#testimonios" class="item">Testimonios</a>
        <a href="#faq" class="item">FAQ</a> %>
        <%= render 'layouts/shared/navbar' %>
 
    </header>

    <!-- Contenido principal -->
    <main style="margin-top: 5em;">
      <div class="ui container">
        <%= yield %>
      </div>
    </main>

    <!-- Pie de página -->
    <footer class="ui vertical footer segment footer-custom">
      <div class="ui center aligned container">
        <div class="ui stackable grid">
          <%= render "shared/footer" %>
        </div>
    </footer>

    <!-- jQuery (requerido por Semantic UI) -->
    <script src="https://cdn.jsdelivr.net/npm/jquery@3.6.0/dist/jquery.min.js" data-turbo-track="reload"></script>

    <!-- Semantic UI JS -->
    <script src="https://cdn.jsdelivr.net/npm/semantic-ui@2.5.0/dist/semantic.min.js" data-turbo-track="reload"></script>

    <!-- Inicializar componentes UI -->
    <script>
      $(document).ready(function() {
        $('.ui.accordion').accordion();
        $('.ui.dropdown').dropdown();
      });
    </script>


    <div id="tsparticles"></div>


    <!-- tsParticles para animación de fondo -->
    <script src="https://cdn.jsdelivr.net/npm/tsparticles@3/tsparticles.bundle.min.js"></script>
    <script>
      tsParticles.load("tsparticles", {
        fullScreen: { enable: false },
        background: {
          color: { value: "#f9fafb" }
        },
        particles: {
          number: { value: 50, density: { enable: true, area: 800 } },
          color: { value: "#8e2de2" },
          shape: { type: "circle" },
          opacity: { value: 0.15 },
          size: { value: 3 },
          move: {
            enable: true,
            speed: 0.6,
            direction: "none",
            outModes: { default: "out" }
          }
        },
        interactivity: {
          events: {
            onHover: { enable: false },
            onClick: { enable: false }
          }
        },
        detectRetina: true
      });
    </script>

    <div id="tsparticles"></div>



    <script>
      document.addEventListener("DOMContentLoaded", () => {
        const phrases = [
          "Campañas inteligentes que crecen con vos",
          "Emails segmentados que convierten",
          "Automatizá tu marketing fácilmente",
          "Analizá tus campañas en tiempo real",
          "Diseñá correos atractivos sin esfuerzo",
          "Aumentá tus ventas con MailerAction",
          "Envía correos a los rubros que te interesan",
          "Tus correos llegan a la bandeja correcta",
          "Envía correos a los rubros que te interesan",


        ];

        let current = 0;
        const el = document.getElementById("dynamic-heading");

        setInterval(() => {
          // Fade out (desvanecer suavemente)
          el.classList.add("fade-out");

          // Espera a que termine la animación
          setTimeout(() => {
            current = (current + 1) % phrases.length;
            el.textContent = phrases[current];

            // Fade in natural al remover la clase
            el.classList.remove("fade-out");
          }, 1000); // debe coincidir con el tiempo del transition
        }, 5000); // cada 5 segundos
      });
    </script>

<script>
  function toggleMobileMenu() {
    const menu = document.getElementById("mobileMenu");
    menu.classList.toggle("active");
  }
</script>

  </body>
</html>
