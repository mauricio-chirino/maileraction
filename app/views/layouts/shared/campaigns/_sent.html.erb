<turbo-frame id="sent_campaigns">
  <div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
      <h5 class="mb-0">📤 Campañas Enviadas</h5>
      <small class="text-muted">Historial de campañas enviadas con éxito</small>
    </div>

    <div class="card-body table-responsive">
      <% if @sent_campaigns.present? %>
        <table class="table table-hover align-middle mb-0">
          <thead class="table-light">
            <tr>
              <th>Título</th>
              <th>Fecha de Envío</th>
              <th>Zona Horaria</th>
              <th>Emails Enviados</th>
              <th class="text-end">Acciones</th>
            </tr>
          </thead>
          <tbody>
            <% @sent_campaigns.each do |campaign| %>
              <tr>
                <td><strong><%= campaign.subject %></strong></td>
                <td><%= l(campaign.send_at, format: :long) if campaign.respond_to?(:send_at) %></td>
                <td><%= campaign.time_zone %></td>
                <td><%= campaign.email_limit || "-" %></td>
                <td class="text-end">
                  <%= link_to "Ver estadísticas", campaign_stats_path(campaign), class: "btn btn-sm btn-outline-info" %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      <% else %>
        <div class="text-center my-4">
          <em>No hay campañas enviadas aún.</em>
        </div>
      <% end %>
    </div>
  </div>
</turbo-frame>

