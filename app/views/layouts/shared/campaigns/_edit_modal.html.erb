<turbo-frame id="modal">
  <div class="modal-header">
    <h5 class="modal-title" id="modalLabel">🕒 Reprogramar Campaña</h5>
    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
  </div>

  <%= form_with model: @campaign, url: campaign_path(@campaign), method: :patch, data: { turbo_frame: "scheduled_campaigns" } do |f| %>
    <div class="modal-body">
      <div class="mb-3">
        <%= f.label :subject, "Asunto de la Campaña", class: "form-label" %>
        <%= f.text_field :subject, class: "form-control", required: true %>
      </div>

      <div class="mb-3">
        <%= f.label :send_at, "Nueva Fecha y Hora de Envío", class: "form-label" %>
        <%= f.datetime_local_field :send_at, class: "form-control", required: true %>
      </div>

      <div class="mb-3">
        <%= f.label :time_zone, "Zona Horaria", class: "form-label" %>
        <%= f.select :time_zone, ActiveSupport::TimeZone.all.map { |tz| [tz.name, tz.name] },
                      {}, class: "form-select", required: true %>
      </div>
    </div>

    <div class="modal-footer">
      <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancelar</button>
      <%= f.submit "Guardar Cambios", class: "btn btn-primary" %>
    </div>
  <% end %>
</turbo-frame>
